# Plan de Documentación — agentic_runtime (Obsidian)

Estado general: `[x] completado`
Estado actual: `[x] completado` — U7 cerrada (checklist integrador). **Plan completo: E0 + T1–T19 técnica + U1–U7 uso.**

## Objetivo

Documentar `agentic_runtime` en el vault de Obsidian, **una página por etapa**, de modo que al
terminar cada etapa se pueda `/clear` sin perder el hilo. La técnica va primero; las guías de uso
(cómo lo implementa un tercero) van al final, con especial cuidado.

- **Repo:** `/home/noheroes/python/agentic_runtime` (paquete `src/agentic_runtime`).
- **Destino Obsidian:** carpeta `agent-runtime/` del vault (vía MCP `obsidian`). Índice:
  `agent-runtime/index.md`. Páginas técnicas en `agent-runtime/wiki/<slug>.md`; guías de uso en
  `agent-runtime/wiki/uso/<slug>.md`.
- **Proyecto hermano:** `agent-llm/` documenta `agentic_models` (el runtime lo consume vía
  `models/caller.py`). Enlazar con `[[agent-llm/index]]` donde aplique.

## Cómo retomar tras un `/clear` (protocolo de cada sesión)

> **REGLA DURA (usuario 2026-06-18):** documentar **NUNCA** corre tests ni valida lint. Eso ya
> se hizo y no se toca. Verificar = **leer** el código, jamás ejecutarlo. El "estado de
> tests/lint" del índice se toma del estado ya conocido (354 passed; lint = ruff+mypy+bandit),
> no re-ejecutando nada.

1. Leer este archivo: ir al **§Registro De Avance** y a la **tabla de etapas**; identificar la
   primera etapa `[ ]`.
2. Cargar las tools del MCP de Obsidian con `ToolSearch` (`select:mcp__obsidian__vault_write,
   mcp__obsidian__vault_read,mcp__obsidian__vault_patch,mcp__obsidian__vault_list,
   mcp__obsidian__vault_get_document_map`).
3. Leer **solo** las "Fuentes" listadas para esa etapa (no más — el objetivo es no saturar).
4. Escribir la página destino con `vault_write` (crea o reemplaza). Seguir la **Convención de página**.
5. Actualizar la TOC en `agent-runtime/index.md` (añadir el `[[wikilink]]` de la página nueva).
6. Marcar la etapa `[x]` en la tabla + una línea en el **§Registro De Avance** (el WHY/lo verificado,
   no el WHAT). Luego `/clear` y repetir.

## Convención de página (estándar del vault)

- **Verificado contra código.** Cada afirmación sale de leer el código actual, no de memoria.
- Encabezado con un bloque **Fuente** que cite los archivos exactos leídos (ruta relativa al paquete;
  `archivo.py:línea` cuando se referencia algo puntual) y la fecha de verificación.
- Idioma español (Perú). Enlaces internos con `[[wikilink]]`. Tono de referencia, no tutorial salvo
  en `wiki/uso/`.
- No documentar lo trivial (imports, `__init__` vacíos); documentar contratos, responsabilidades,
  invariantes, puntos de extensión y "qué inyecta el integrador".
- Plantilla mínima:
  ```markdown
  # <Título>

  > **Fuente:** `tools/dispatcher.py`, `tools/pool.py` — verificado contra código 2026-06-18.

  <cuerpo>

  ## Referencias cruzadas
  - [[agent-runtime/index]]
  ```

## Etapas (1 página por etapa)

### Bloque 0 — Setup

| # | Página destino | Fuentes a leer | Criterio de cierre |
|---|---|---|---|
| E0 [x] | `agent-runtime/index.md` (actualizar) | `pyproject.toml`, este plan, [[plan2-capabilities-state]] | Índice con repo path correcto (`agentic_runtime`, NO `agent-runtime`), estado de tests/lint tomado del estado YA conocido (354 passed; lint = ruff+mypy+bandit) **sin ejecutar nada**, y TOC con todas las páginas planeadas (como enlaces aún por crear) |

### Bloque 1 — Técnica

| # | Página destino | Fuentes a leer | Criterio de cierre |
|---|---|---|---|
| T1 [x] | `wiki/arquitectura.md` | `__init__.py`, `factory.py`, `contracts/runtime.py`; de contexto `PLAN_RUNTIME_AGENTICO.md` y `PLAN_COMPLEMENTARIO_RUNTIME.md` | Visión general: qué es/no es (runtime agnóstico, sin entry point/HTTP/auth), principios D1–D5, capas, diagrama del flujo de un turno |
| T2 [x] | `wiki/factory.md` | `factory.py`, `contracts/runtime.py` | `create_runtime()` + `RuntimeConfig`/`CapabilitiesConfig`: qué ensambla, qué exige al integrador, qué trae por defecto |
| T3 [x] | `wiki/contratos.md` | `contracts/{runtime,permissions,storage,compaction,user_input}.py` | Catálogo de Protocols de frontera y quién los implementa (runtime vs integrador) |
| T4 [x] | `wiki/loop.md` | `loop/{agent_loop,protocol,basic,factory}.py` | El loop: fases del turno, ensamblaje de pool por turno, aplicación de `context_modifier`, cierre |
| T5 [x] | `wiki/modelos-caller.md` | `models/{caller,protocol}.py` | `ModelCallerProtocol` + `AgenticModelsCaller` (puente a `agent-llm`); `system_sections`, `model_id` por request. Enlazar `[[agent-llm/index]]` |
| T6 [x] | `wiki/eventos.md` | `events/{bus,event_types,protocol}.py` | Bus de eventos, taxonomía de eventos (TokenEvent, tool, etc.), cómo se suscribe el integrador |
| T7 [x] | `wiki/tools-core.md` | `tools/{protocol,dispatcher,pool,registry,native_registry,factory,exec_env,deferred}.py` | Modelo de tools: protocolo, dispatch desde `ctx.tool_pool`, `assemble_tool_pool` (prefijo nativo/dedup/deny), deferred/ToolSearch, exec env inyectable |
| T8 [x] | `wiki/tools-nativas.md` | `tools/native/*.py` (17 archivos) | Catálogo de tools nativas: propósito + permisos de cada una (bash, read/write/edit, glob/grep, agent, task_tools, todo_write, plan_mode, worktree, web_*, ask_user, sleep, config, tool_search) |
| T9 [x] | `wiki/contexto.md` | `context/{tool_use,execution,presentation,adapters}.py` | `ToolUseContext`/`ExecutionContext`, `PathPresentation` (choke point), adapters; qué estado viaja por turno |
| T10 [x] | `wiki/ejecucion.md` | `execution/runner.py`, `execution/local/{runtime,notification,summarizer}.py` | `LocalAgentRuntime`, runner, canal de notificación de background, summarizer/compactación |
| T11 [x] | `wiki/sesiones-tareas.md` | `execution/session/{protocol,session}.py`, `execution/tasks/{registry,status}.py`, `execution/observer/{events,observer}.py`, `execution/fork/__init__.py`, `execution/context/agent_context.py` | Sesión, registro de tareas + estados, observer, fork de contexto, agent_id |
| T12 [x] | `wiki/modos-senales.md` | `modes/{manager,protocols}.py`, `signals/{bus,protocols}.py` | ModeManager (transición de modo), SignalBus (abort/cascade); estado actual de cableado |
| T13 [x] | `wiki/hooks.md` | `hooks/{protocol,runner}.py` | Sistema de hooks: protocolo, runner, puntos de invocación |
| T14 [x] | `wiki/storage.md` | `storage/{protocol,filesystem,factory}.py`, `contracts/storage.py` | `StorageProtocol`, default filesystem, factory/registry, taxonomía `StorageKeys` (dos planos) |
| T15 [x] | `wiki/capabilities-core.md` | `capabilities/{protocol,contracts,manager,resolver}.py` | Modelo de capability: `CapabilityProvider`, `CapabilityManager`, convergencia native↔capability (`build_tool_pool`), `CapabilitiesResolver` |
| T16 [x] | `wiki/capability-mcp.md` | `capabilities/mcp/*.py` (10 archivos) | Provider MCP: cliente (stdio/http), config + auth (estrategias none/bearer/oauth), state, tool_adapter, resources, stores; lo que inyecta el integrador (TokenStorage, handlers) |
| T17 [x] | `wiki/capability-skills.md` | `capabilities/skills/*.py` (8 archivos) | Provider Skills: frontmatter, loader, SkillTool, context modifier, catálogo, slash commands, store, base_dir |
| T18 [x] | `wiki/capability-memoria.md` | `capabilities/memory/*.py` (5 archivos) | Provider Memoria: store filesystem, recall, prompt section, scope (`main` vs subagente), guardado canónico vía `write_file` |
| T19 [x] | `wiki/capability-voz.md` | `voice/{protocol}.py`, `factory.py` (VoiceConfig + `_wire_tts`/`_resolve_prompt`) | Capability de borde STT/TTS: puertos, plomería en el runtime, activación por config, barge-in |

### Bloque 2 — Uso (cómo implementarlo — especial atención)

| # | Página destino | Fuentes a leer | Criterio de cierre |
|---|---|---|---|
| U1 [x] | `wiki/uso/quickstart.md` | `factory.py`, `scripts/` o `tests/test_runtime_e2e.py` | Mínimo viable: `create_runtime()` → despachar una task → leer el resultado/stream. Ejemplo copy-paste verificado |
| U2 [x] | `wiki/uso/inyeccion-dependencias.md` | `contracts/*.py`, `factory.py` | Qué DEBE proveer el integrador vs qué trae por defecto: model caller, storage, presentation, permissions, exec env. Tabla puerto→default→cuándo reemplazar |
| U3 [x] | `wiki/uso/eventos-streaming.md` | `events/*.py`, un test de loop | Suscribirse a eventos, stream de tokens, eventos de tool; patrón para una UI/relay |
| U4 [x] | `wiki/uso/activar-capabilities.md` | `factory.py` (`CapabilitiesConfig`), tests E2E de capabilities | Habilitar MCP servers, skill dirs, memoria y voz por config; sembrar permisos (`initial_allowed_tools`) |
| U5 [x] | `wiki/uso/subagentes-background.md` | `tools/native/{agent,task_tools}.py`, `execution/{tasks,fork,local/notification}` | Lanzar subagentes, background tasks, fork de contexto, recibir notificaciones |
| U6 [x] | `wiki/uso/permisos-y-modos.md` | `contracts/permissions.py`, `tools/native/{plan_mode,worktree}.py`, `modes/` | Modelo de permisos, plan mode, worktree; cómo gobernar qué puede hacer el agente |
| U7 [x] | `wiki/uso/checklist-integrador.md` | revisión de las páginas anteriores | Checklist de integración end-to-end + errores comunes + enlaces a todas las páginas. Cierre del plan |

## Mapeo etapa → página (resumen para la TOC del índice)

Técnica: arquitectura, factory, contratos, loop, modelos-caller, eventos, tools-core, tools-nativas,
contexto, ejecucion, sesiones-tareas, modos-senales, hooks, storage, capabilities-core,
capability-mcp, capability-skills, capability-memoria, capability-voz.
Uso: quickstart, inyeccion-dependencias, eventos-streaming, activar-capabilities,
subagentes-background, permisos-y-modos, checklist-integrador.

## Registro De Avance

<!-- Una línea por etapa cerrada: fecha — etapa — qué se verificó / decisiones. El WHY, no el WHAT. -->
- 2026-06-19 — U7 — `wiki/uso/checklist-integrador.md` escrita (séptima y ÚLTIMA guía de **Uso**) como **página de cierre/síntesis**: fuente = revisión de las seis guías U1–U6 (NO se re-leyó código — todas ya verificadas contra fuente 06-18/06-19; la página declara explícitamente que no introduce hechos nuevos, solo consolida). **Eje rector destilado de las seis: el runtime se ensambla por inyección y TRES cosas son cableado explícito que el factory NO hace — (1) `runtime.startup()` abre la red de capabilities, (2) `set_runner`/`set_registry` para subagentes, (3) drenar el canal de notificaciones background; olvidar una de las tres origina casi todos los errores.** Estructura: checklist end-to-end en 5 fases (0 construir mínimo→`model_caller` único puerto sin default / 1 consumir salida→no tocar bus, `result()` descarta TokenEvent, `DoneEvent`=fin de turno no de task, no hay join público / 2 activar capabilities→gate por presencia + `startup()` + MCP diferidas+permiso + `write_file` para memoria + voz / 3 gobernar→`initial_allowed_tools` palanca real, 3 barreras, plan mode y worktree son señales no candados, `ModeManager` no cableado / 4 subagentes→runner+registry compartido, fork `inherit_messages=False`, drenaje no automático) + tabla consolidada de 16 errores comunes (cada uno → guía donde se desarrolla) + **mapa de TODA la documentación** (7 guías de uso + las 19 técnicas con wikilink). TOC del índice YA tenía el wikilink de checklist-integrador (Uso #7, sin cambio). **CIERRA el Bloque 2 (Uso) y el PLAN COMPLETO** (E0 + T1–T19 + U1–U7); estado general marcado `[x]`. Verificación = lectura (de las páginas previas), sin ejecutar nada.
- 2026-06-19 — U6 — `wiki/uso/permisos-y-modos.md` escrita (sexta guía de **Uso**, tono tutorial) y verificada por lectura de `contracts/permissions.py`, `tools/native/{plan_mode,worktree,bash}.py`, `modes/{manager,protocols,__init__}.py` + los call sites del permiso (`tools/pool.py:48-74`, `tools/dispatcher.py:55-84`, `loop/agent_loop.py:108-131`, `execution/local/runtime.py:186-196`, `factory.py:92,203`) y grep de consumo de los marcadores. **Eje rector: de las cuatro palancas para gobernar al agente, SOLO los permisos son un candado real; plan mode y worktree son protocolos de SEÑALIZACIÓN (marcador en `app_state.native` + instrucción al modelo) que el runtime NO hace cumplir por sí mismo; `ModeManager` ni siquiera está cableado (T12).** Hallazgos que dirigen la página: (1) **`PermissionContext` es agnóstico de proveedor** (solo nombres de tools): tres listas (`always_allow_command`/`always_allow_session`/`always_deny`); **`allowed_names()` UNE las dos allow → hoy el runtime NO distingue command de session** (la distinción es semántica heredada del canónico, no comportamiento); `with_command_allow` devuelve copia con dedup. (2) **el permiso muerde en TRES barreras** tabuladas: deny se aplica en el ENSAMBLADO (`pool.py:59` → la denegada ni entra al pool = no existe), `requires_permission` sin conceder solo se OCULTA del anuncio (`agent_loop.py:123` = modelo autónomo) Y se rechaza en EJECUCIÓN (`dispatcher.py:62` → "permiso denegado", segunda barrera contra diferidas descubiertas por ToolSearch); subrayada la distinción deny (elimina) ≠ no-conceder (oculta pero en pool). (3) **siembra**: no hay puerto Protocol suelto — el principal POSEE su `PermissionContext`, sembrado por `RuntimeConfig.initial_allowed_tools` → `runtime.py:189-195`; caminos que confluyen (integrador / memoria necesita `write_file` / skills `with_command_allow` al activarse, cruce S2↔M3 / subagentes heredan por snapshot del fork); reflejo automático porque el pool se reensambla por turno. (4) **plan mode — HALLAZGO HONESTO**: `app_state.native["plan_mode"]` NO se consulta en NINGÚN punto del loop/exec (grep: único lector = un comentario en `agent_loop.py:276`) → el runtime NO bloquea write/Edit en plan mode ni pausa esperando aprobación; la restricción vive en la INSTRUCCIÓN al modelo; convertirlo en candado = el integrador lee el marcador y añade las escrituras a `always_deny`. `EnterPlanMode` se rechaza en subagente (`agent_id is not None`), `requires_permission=False`. (5) **worktree — mismo patrón**: crea git worktree real por subprocess (`requires_permission=True`, hay que sembrarlo) y guarda `worktree_session={path,branch,original_cwd}`, PERO `bash` corre `exec_env.run_shell(command)` SIN cwd del worktree (`bash.py:25-29`) → el runtime NO hace chdir; el dir aislado existe pero encaminar el trabajo es del agente (`cd`) o del `exec_env`. (6) **`ModeManager` no cableado** (remite a [[modos-senales]]): para background/fork hoy se usa `is_backgrounded` + `ForkPolicy`, no `AgentMode`; queda como extension primitive `on_transition`. Cierra con tabla "¿es un candado?" + errores comunes (concedí write y no se usa / denegué y reaparece / esperaba que plan mode bloqueara / bash fuera del worktree). TOC del índice YA tenía el wikilink de permisos-y-modos (Uso #6, sin cambio). Verificación = lectura, sin ejecutar nada.
- 2026-06-18 — U5 — `wiki/uso/subagentes-background.md` escrita (quinta guía de **Uso**, tono tutorial) y verificada por lectura de `tools/native/{agent,task_tools}.py`, `execution/runner.py`, `execution/fork/__init__.py`, `execution/tasks/{registry,status}.py`, `execution/local/{runtime,notification}.py`, `contracts/runtime.py` (`RuntimeTask`) + grep de cableado y los e2e `test_runtime_e2e.py::{test_e2e_background_subagent_notifies_and_persists,test_e2e_fork_isolates_parent_messages}`. **Eje rector: hay DOS formas de lanzar subagente (modelo→tool `Agent` vs integrador→`dispatch(parent_snapshot=...)`) y AMBAS convergen en `RuntimeContextForker` + canal de notificación; la diferencia es quién dispara.** Hallazgos que dirigen la página: (1) **el factory NO cablea ni runner ni task-registry globales** — `set_runner()`/`set_registry()` son trabajo del integrador; sin ellos la tool `Agent` y las `Task*` lanzan `RuntimeError` ("not initialized"). El grep confirma que `set_runner`/`set_registry` NO se llaman en src no-test. (2) **mismatch de firma runner↔runtime**: `SubagentRunnerProtocol.run(fork_ctx, *, background)` ≠ `LocalAgentRuntime.dispatch(task, parent_snapshot=...)` → el integrador provee un ADAPTADOR delgado que traduce `ForkContext→(RuntimeTask, ForkSnapshot)` (documentado con snippet ilustrativo que usa `_task_registry`/`result` como en U1, sin join público). (3) **`AgentTool`**: `requires_permission=False`+`safe_for_background=True`, tope duro `_MAX_SUBAGENT_DEPTH=5` chequeado contra `ctx.subagent_depth`, 5 params (prompt/description/run_in_background/model_override/inherit_messages); arma `ForkSnapshot` del ctx actual y llama `get_runner().run(...)` sin conocer al runtime. (4) **fork**: `ForkPolicy` defaults verificados (`inherit_messages=False`=AISLAMIENTO por defecto, permisos+pool heredados, `propagate_abort=True`=misma `asyncio.Event`→cascada de abort); desde `RuntimeTask` el único dial es `fork_context: bool`→`inherit_messages`; `RuntimeContextForker.fork` genera agent_id nuevo SIEMPRE pero conserva session_id (concuerda con T11). (5) **background = visibilidad, no capacidad** (`is_backgrounded` no toca el toolset, B3); las 6 tools `Task*` operan vía `get_registry()`. **Matiz que muerde y se subraya**: el `self._task_registry` del runtime ≠ el singleton de módulo que leen las `Task*` → inyectar la MISMA instancia en `set_registry()` Y `RuntimeConfig.task_registry`. (6) **notificaciones**: el hijo `put_notification` por `parent_session_id` en las 3 salidas (completed/failed/killed; completed se condensa con `summarize_if_needed`, tope 2000, degradación graceful), sin ref viva a la `Session` del padre; el padre `drain_notifications`+`process_background_notification` (actualiza `BackgroundTaskRef` + inyecta `<task-notification>` rol user). **Hallazgo honesto subrayado: el drenaje NO es automático** — el `AgentLoop` expone `register_turn_start_hook` (DrainableLoop) PERO `LocalAgentRuntime` no registra ninguno; vaciar+reinyectar es del integrador (display/persistencia NO son de `process_background_notification`, G2/D4; grep confirma cero `register_turn_start_hook` en src no-test). Cierra con tabla de errores comunes. Corregido un wikilink a `[[wiki/fork]]` inexistente → `[[sesiones-tareas]]` (el fork se documentó en T11). TOC del índice YA tenía el wikilink de subagentes-background (Uso #5, sin cambio). Verificación = lectura, sin ejecutar nada.
- 2026-06-18 — U4 — `wiki/uso/activar-capabilities.md` escrita (cuarta guía de **Uso**, tono tutorial) y verificada por lectura de `factory.py` (`CapabilitiesConfig` `:33-51`, `VoiceConfig` `:60-71`, `RuntimeConfig` `:75-92`, `_build_capability_manager` `:107-146`, gate de voz en `_build_local` `:183-187`) + los E2E `test_capability_wiring.py`, `test_capability_registration_e2e.py`, `test_memory_loop_e2e.py`, `test_voice_io.py`. **Eje rector: las capabilities se DECLARAN por config y el factory las activa por GATE DE PRESENCIA — el runtime no las cablea a mano.** Toda la declaración cabe en dos campos de `RuntimeConfig` (`capabilities: CapabilitiesConfig` para MCP/Skills/Memoria, los tres providers; `voice: VoiceConfig` para STT/TTS, que NO es provider sino plomería de borde). Hallazgos que dirigen la página: (1) **condiciones de gate exactas leídas del factory** tabuladas — MCP sii `mcp_servers` no vacío **o** `mcp_config_store`; Skills sii `skill_dirs` **o** `skill_store`; Memoria sii `memory_root` **o** `memory_store`; `extra_providers` siempre; voz por su gate aparte (`stt/tts` activos sii primitiva inyectada **Y** flag `*_enabled`). (2) **el factory NO abre red**: los servers MCP se declaran pero se CONECTAN en `runtime.startup()` (lo invoca el integrador) — sin startup no hay tools MCP; subrayado con el patrón de `test_factory_builds...` (registrado, `state.servers` poblado, sin conectar) vs `test_runtime_startup_connects...`. (3) **dos avisos MCP que cambian el turno**: tools diferidas (M3, no anunciadas hasta `ToolSearch`, pero en el pool y ejecutables — `mcp_ping` en `ctx.tool_pool` sin anunciarse) y `requires_permission=True` (hay que concederlas, §6); server inalcanzable se AÍSLA (`failed`) sin tumbar al resto. (4) **el integrador MAPEA su JSON al contrato** ([[integrator-maps-to-contract]]): `_extract_mcp` extrae/traduce el registro externo a `McpServerConfig` antes de persistir, no se mete crudo. (5) **`initial_allowed_tools` (en `RuntimeConfig`, NO en CapabilitiesConfig) es la pieza que hace USABLES varias capabilities**: memoria necesita `write_file` (no hay tool `remember`; sin sembrar = solo lectura, el principal no se auto-concede), MCP se concede aquí o vía `allowed-tools` de una skill al invocarla (cruce S2↔M3: `office` declara `allowed-tools: echo_upper` → tras `Skill(office)` la MCP diferida `echo_upper` queda anunciada+ejecutable); subagentes heredan por snapshot del fork. (6) **voz**: STT transcribe `audio_prompt` de la `RuntimeTask` a prompt normal con degradación graceful (sin STT/flag off/sin audio→`task.prompt`); TTS deriva cada `TokenEvent` saneado por `PathPresentation`, incremental, `flush` al cerrar, SOLO el principal habla; flags mutean un canal sin retirar la implementación. Receta combinada copy-paste (MCP+Skills+Memoria+Voz+permisos) + cierre con "lo que NO hace el factory" (red/permisos/deformar JSON). TOC del índice YA tenía el wikilink de activar-capabilities (Uso #4, sin cambio). Verificación = lectura, sin ejecutar nada.
- 2026-06-18 — U3 — `wiki/uso/eventos-streaming.md` escrita (tercera guía de **Uso**) y verificada por lectura de `events/{protocol,event_types,bus,__init__}.py`, `execution/local/runtime.py:118-163` (`dispatch`/`stream`) y los tests `test_runtime_e2e.py::{test_e2e_stream_surfaces_full_event_sequence,test_e2e_multi_turn_tool_call}` + `test_events.py`. **Eje rector: el integrador NUNCA toca el `EventBus`** (es interno) — su frontera son DOS métodos del runtime: `dispatch(task, *, on_event=EventHandler)` (no bloquea) y `stream(task)` (azúcar sobre `dispatch`, async iterator que no pierde eventos por el centinela en `add_done_callback`); `on_event` se registra vía `subscribe_all` → recibe TODOS los eventos, se filtra por `isinstance` dentro del handler (el bus NO se expone para `subscribe` selectivo). Dos avisos que dirigen la página (los bugs reales de un relay): (1) **`result()`/`rec.events` descartan los `TokenEvent`** → para texto en vivo es obligatorio `on_event`/`stream`, `result()` solo da el acumulado final; (2) **`DoneEvent` cierra un TURNO, no la task** — `stop_reason=="tool_calls"` significa que el loop sigue (otro `DoneEvent` vendrá); el cierre real es el fin del iterador, no el primer `DoneEvent` (si el relay corta ahí, corta a mitad). Taxonomía tabulada (Token/ToolCall/ToolResult/Done/Error; `Usage` viaja DENTRO de `DoneEvent`, no es Event) con el origen ya fijado en [[eventos]]: 4 nacen en el caller y el loop los reemite, `ToolResultEvent` lo FABRICA el loop casando `call_id`. Patrón A (`stream()` → relay SSE, recomendado) y Patrón B (`dispatch(on_event=...)` cuando no se itera, con el recordatorio de que NO hay join público — `stream()` o `_task_registry...asyncio_task`, concuerda con U1). Reconstrucción de texto = `"".join(TokenEvent.content)` == `result()` al cierre (patrón del test). Cierra con extensión D5: `subscribe`/`emit` resuelven por tipo EXACTO (no `isinstance`) → eventos propios por bus propio, pero el lever del consumidor del runtime sigue siendo `on_event`+filtrado. Errata de codificación (cirílico en «pidió») detectada y corregida reescribiendo la página entera con `vault_write` (el targeting por heading acentuado de `vault_patch` falla). TOC del índice YA tenía el wikilink de eventos-streaming (Uso #3, sin cambio). Verificación = lectura, sin ejecutar nada.
- 2026-06-18 — U2 — `wiki/uso/inyeccion-dependencias.md` escrita (segunda guía de **Uso**) y verificada por lectura de `factory.py` (`RuntimeConfig`/`CapabilitiesConfig` + `_build_local` defaults `:148-206`) y `contracts/{runtime,storage,permissions,user_input,compaction}.py`. **Eje rector: un SOLO puerto carece de default útil (`model_caller`); todo lo demás el factory lo resuelve con un default sano** → `create_runtime()` desnudo construye un runtime local funcional salvo que no puede llamar al modelo. Hallazgos que dirigen la página: (1) **dos mecanismos de inyección distintos** — por `RuntimeConfig` al construir (el grueso) vs por `ToolUseContext` durante el turno (`StorageContract`, sin campo en config ni default, frontera pura que el integrador adjunta al ctx si sus tools resuelven rutas); se documenta la distinción explícita. (2) **aviso anti-confusión de los dos «storage»** retomado de T3/T14: `StorageProtocol` (KV durable, default `FilesystemStorage` vía `config.storage`) ≠ `StorageContract` (rutas+I/O de tools, sin default, por contexto). (3) **tabla puerto→contrato→default→cuándo reemplazar** con los 5 ejes del criterio al frente (model caller / storage / presentation / permissions / exec env) + el resto (hook_runner None=no-op, task_registry None=registro en-proceso, tools.extras, capabilities con gate por presencia, user_input=`NoopUserInputProcessor`, compaction la aportan capabilities). (4) **permisos no es un puerto Protocol inyectado suelto** sino `initial_allowed_tools: list[str]` que siembra el `PermissionContext` del principal (que el runtime POSEE) — espejo del `Write` canónico, razón del `write_file` para memoria (concuerda con T2/T14/T18). (5) defaults exactos leídos del factory: `IdentityPresentation()` (`:178`), `LocalExecEnvironment()` (`:181`), `StorageRegistry.create(backend, ...)` (`:157`); contratos `Protocol` `@runtime_checkable` = el integrador implementa por forma, sin heredar. TOC del índice YA tenía el wikilink de inyeccion-dependencias (Uso #2, sin cambio). Verificación = lectura, sin ejecutar nada.
- 2026-06-18 — U1 — `wiki/uso/quickstart.md` escrita (primera guía de **Uso**, tono tutorial) y verificada por lectura de `factory.py`, `contracts/runtime.py:15-29` (`RuntimeTask`), `execution/local/runtime.py:118-174` (dispatch/stream/status/result/cancel), `tests/test_runtime_e2e.py` y `scripts/e2e_runtime_test.py`. **Eje rector: la integración mínima son TRES movimientos** — `create_runtime(config=...)` → `dispatch(RuntimeTask)` → leer resultado/stream; el ejemplo copy-paste es el cableado real del e2e reducido (caller faux `ScriptedCaller` + `EchoTool`, sin red, salida determinista `result="terminado"`). Hallazgos que dirigen la página y se documentan con honestidad: (1) el **único puerto sin default útil es `model_caller`** (contrato de un método `complete(messages, tools, *, stop, model_id)` que devuelve un async generator de `Event`); todo lo demás lo arma el factory con defaults. (2) **NO existe join público**: `dispatch()` no bloquea (lanza `ensure_future`, devuelve `task_id`), y para esperar hay que alcanzar `runtime._task_registry.get(task_id).asyncio_task` — exactamente lo que hacen tests y script; se documenta como tal y se ofrece `stream()` como la vía pública que sí drena hasta el cierre (azúcar sobre `dispatch(on_event=...)`, no pierde eventos). (3) variante de stream reconstruye el texto acumulando `TokenEvent.content` (patrón del test `test_e2e_stream_surfaces_full_event_sequence`); el `ToolResultEvent` lo **fabrica el loop**, no el caller. (4) salto a modelo real = cambiar SOLO el `model_caller` por `AgenticModelsCaller` (+`model_id`), resto intacto. Tabla de `RuntimeTask` (obligatorios `prompt`/`description`; resto opcional con default 50 turnos/300 s). TOC del índice YA tenía el wikilink de quickstart (Uso #1, sin cambio). Verificación = lectura, sin ejecutar nada. **Abre el Bloque 2 (Uso).**
- 2026-06-18 — T19 — `wiki/capability-voz.md` escrita y verificada por lectura de `voice/{protocol,__init__}.py`, `factory.py` (`VoiceConfig` + gate `:185-187`), `execution/local/runtime.py` (`_resolve_prompt` `:198-210`, `_wire_tts` `:212-240`, call sites `:298-312`, ctor `:55-92`) y `contracts/runtime.py:15-29` (`audio_prompt`). **Eje rector: la voz es la ÚNICA capability de *borde de I/O* y NO es un `CapabilityProvider`** — no se registra en el manager, no aporta tools/catálogo/contexto/prompt section; es pura plomería alrededor del turno (contraste explícito con MCP/Skills/Memoria). Hallazgos que dirigen la página: (1) **dos puertos `Protocol` `@runtime_checkable`**: STT `transcribe(AudioInput, ctx)->str` (runtime agnóstico a códec/origen; `AudioInput` lleva `data`+`mime_type`+`sample_rate`) y TTS `speak`/`flush` (salida INCREMENTAL baja latencia; **el runtime NO reproduce audio**). (2) **gate en el factory** (`stt/tts` activos *sii* primitiva inyectada Y flag `*_enabled`, flags default True): permite mutear un canal sin retirar la implementación; el runtime recibe primitiva o `None`, sin lógica de habilitación. (3) **entrada `_resolve_prompt`** una vez por turno antes del loop: audio→`transcribe`, con **degradación graceful** (sin STT/sin audio→`task.prompt`; excepción→log+fallback; transcripción vacía→fallback `text or task.prompt`); la transcripción entra como prompt NORMAL (modelo agnóstico al origen); `audio_prompt` viaja en `RuntimeTask`, espejo de `prompt` que queda de fallback. (4) **salida `_wire_tts`** suscribe TTS al bus antes de `loop.run`: **guarda `ctx.is_subagent`→solo el principal habla**; `TokenEvent`→`speak` PERO el texto pasa antes por `presentation.sanitize_output` (choke point de [[contexto]] — nunca se leen rutas reales de infra), vacío se descarta; `DoneEvent`→`flush` SALVO `stop_reason=="tool_calls"` (el turno sigue → no vaciar); `speak`/`flush` en try/except con log (TTS roto no tumba el turno). (5) **barge-in NO implementado** (grep `barge`=0): se documenta honestamente como punto de extensión del integrador apoyado en el abort existente (`ctx.stop`); el runtime no provee VAD ni `stop()` sobre el TTS (no reproduce audio → no lo detiene). TOC del índice YA tenía el wikilink de capability-voz (#19, sin cambio). Verificación = lectura, sin ejecutar nada. **Cierra el Bloque 1 (Técnica).**
- 2026-06-18 — T18 — `wiki/capability-memoria.md` escrita y verificada por lectura de los 5 archivos de `capabilities/memory/` (`provider,prompt,recall,store` + `__init__`). **Eje rector: la memoria es el provider concreto que NO aporta tools** — `tools()`/`catalog()`=`[]`; es **contexto + instrucciones**, no algo seleccionable; el guardado lo hace el modelo con `write_file` (NO hay tool `remember`/`recall`). Contraste explícito con [[capability-mcp]] (aporta tools) y [[capability-skills]] (aporta tool + contexto). Hallazgos que dirigen la página: (1) **dos superficies de activación** del contrato — `system_prompt_section` (bloque ESTABLE cache-friendly: instrucciones + índice `MEMORY.md`, solo varía con el índice; es el hook OPCIONAL invocado tolerante por el manager vía `getattr`, concuerda con T15) y `active_context` (recall por turno, dicts `role:"system"` que el loop rinde como `<system-reminder>`); **`compact_context` delega en `active_context`** (las memorias sobreviven al recorte, igual que Skills). `startup` solo `ensure_dir(None)` (crea el dir del principal para que el destino de `write_file` exista); `shutdown` no-op. (2) **scope** = `agent_id if is_subagent else None`: el principal usa `None`→clave estable `'main'` PORQUE su `agent_id` es uuid por despacho, inservible como clave persistente; subagentes se aíslan por `agent_id` (A no ve B) — concuerda con identidad de T11. (3) **store de SOLO lectura** — `MemoryStore` Protocol `@runtime_checkable` (puerto inyectable como `base_dir`/`StorageProtocol`); default `FilesystemMemoryStore(<root>/<scope>/)` lee índice+cabeceras pero NO escribe; `ENTRYPOINT="MEMORY.md"` se EXCLUYE del `scan` (ya va en el system prompt → no se duplica en recall); `scan` ordena por ruta (determinista), `[]` si no hay dir; **aislamiento por ítem** en `_parse_header` (fichero roto→se salta, sin frontmatter→identidad cae a `path.stem`). (4) `MemoryHeader` (Pydantic) NO carga cuerpo — recall opera sobre `name`/`description`; `path` absoluto = marcador de dedup del loop + ubicación para `read_file`; `mtime`=desempate. (5) `_parse_frontmatter` tolerante propio (NO reutiliza `SkillFrontmatter`: usa `metadata.type`, no `allowed-tools`; solo comparte el enfoque), nunca lanza. (6) **recall DETERMINISTA sin LLM** (`rank_memories`): solapamiento de keywords sobre `name`+`description`, tokens >2 chars, desempate `mtime` desc, sin solapamiento→fuera, query vacía→`[]`; ranker LLM del canónico = opinión inyectable FUTURA. La query la da `_last_user_text` que **salta los `<system-reminder>`** (el propio recall se rinde envuelto en uno → no morderse la cola). (7) **guardado canónico vía `write_file`** (sin tool propia): el ciclo de guardado NO pasa por la capability → el agente necesita `write_file` en el pool, razón del `initial_allowed_tools` del factory (principal autónomo no se auto-concede; subagentes heredan por snapshot — concuerda con T2/T10). TOC del índice ya tenía el wikilink de capability-memoria (#18, sin cambio). Verificación = lectura, sin ejecutar nada.
- 2026-06-18 — T17 — `wiki/capability-skills.md` escrita y verificada por lectura de los 8 archivos de `capabilities/skills/` (`frontmatter,loader,provider,skill_tool,state,store,commands` + `__init__`). Eje rector de la página: **una skill es un `SKILL.md` (frontmatter + cuerpo) y la cadena de carga es TOLERANTE de punta a punta — nada lanza — porque la identidad la garantiza el DIRECTORIO, no el frontmatter** (a diferencia de MCP, aquí NO hay borde de seguridad estricto; el contraste con [[capability-mcp]] dirige la lectura). Hallazgos que dirigen la página: (1) **espectro S0–S5 declarado, no fingido**: las piezas existen todas en código; lo que cambia entre etapas es QUIÉN las cablea — varias las usa el integrador, no el loop (`active_context`/`compact_context`/slash commands). (2) **frontmatter** Pydantic `extra="allow"` (conserva directivas de terceros) con coerción `mode="before"`: 4 campos operativos que **degradan a comportamiento definido, nunca a error** (`name`→dir, `description`→1er párrafo, `allowed-tools`→`[]`, `model` `None`/`inherit`→hereda del padre); `from_raw` y `parse_frontmatter` son construcción TOTAL (BOM/sin cierre/YAML inválido/no-mapping → `{}` + cuerpo intacto). (3) **loader** = salida tipada `SkillDefinition` (nunca dict suelto); `model is None`="hereda" y `allowed_tools` vacío="no activa extra" son convenciones, no ausencias; `load_skills_dir` orden estable por ruta + aislamiento por ítem (un SKILL.md roto se salta). (4) **`base_dir`** (=`skillRoot` del canónico) es el directorio de la skill para localizar bundled `scripts/`/`templates/` por ruta y ejecutarlos vía bash; vacío si se registró solo como contenido; sin manejo especial de subcarpetas. (5) **`SkillTool`** NO diferida + `requires_permission=False` (el modelo la necesita para activar skills); `execute` devuelve `render_skill` (mensaje meta rol `tool`, "no reinvoques") y **adjunta `context_modifier`** al result. (6) **el efecto (S2) es enteramente del modifier** (muta ctx in-place, refleja al reensamblar pool por turno): puebla `caps["active_skills"][name]` con CONTENIDO completo (no solo nombre), añade `allowed_tools` al `PermissionContext` Y `mark_tools_discovered` → **cruce S2↔M3** (una skill habilita tools MCP diferidas `x__*`); el runtime NO deriva tools de `invoked_skills`. (7) `SkillsState`=`appState` de skills (catálogo fuera del runtime, separado del registry nativo); estado de skills INVOCADAS no vive aquí sino scopeado por agente en `ctx.app_state.capabilities`. (8) **provider** estructural: `tools()` da `SkillTool` SOLO si hay skills; `active_context` (S3) reinyecta instrucciones de skills activas como `system` (no el catálogo como mandato de reinvocación); `compact_context` (S5) delega en `active_context` (el contenido sobrevive a compactar); `startup` carga del store con aislamiento por ítem; métodos `load_dir`/`add_skill_text`/`register_skill`/`process_slash_command` son para el integrador, el loop no los importa. (9) **slash commands** (S4) desacoplados del loop: `process_slash_command` aplica el MISMO modifier que la tool `Skill` + `render_skill`; **el fork/background NO lo decide la skill** (lo hace el runtime vía `RuntimeContextForker`, la capability solo activa inline). (10) **`SkillStore`** Protocol `@runtime_checkable` (list/read/write/remove) con default `StorageBackedSkillStore` sobre `StorageProtocol` (`<prefix>/<name>/SKILL.md`), simétrico al store de MCP y a `StorageBackedTokenStorage`, todo IO en try/except con log. TOC del índice YA tenía el wikilink de capability-skills (#17, sin cambio). Verificación = lectura, sin ejecutar nada.
- 2026-06-18 — T16 — `wiki/capability-mcp.md` escrita y verificada por lectura de los 10 archivos de `capabilities/mcp/` (`provider,client,config,auth,state,tool_adapter,resource_tools,config_store,token_storage` + `__init__`). Eje rector: **MCP es el primer `CapabilityProvider` concreto** y el runtime NO sabe que las tools vienen de MCP (las recibe vía el manager; estado MCP SEPARADO del registry nativo, patrón `appState.mcp.*`). Hallazgos que dirigen la página: (1) **dualidad estricto/tolerante recorre todo el paquete** — `add_server`/`parse_server_config` lanzan (borde de seguridad, acción explícita), `load_servers`/`load_server_configs`/`build_mcp_tool` aíslan por ítem con log (config de terceros); `connect_server` aísla fallos (FAILED, no propaga → el resto sigue). (2) **gate y red**: el factory solo registra el provider si hay servers, pero **abrir red es de `startup()`, no del factory** (concuerda con T2); `startup()` carga el registro persistido del `config_store` y conecta TODOS. (3) **config** `McpServerConfig` Pydantic `extra="allow"` = abierto en lo operativo / ESTRICTO en identidad+seguridad: `type`∈{http,sse,stdio} o inferido (exactamente uno command/url), `bearer` exige `token`, **auth bearer/oauth NO aplica a stdio** (credenciales por `env`). (4) **auth = estrategias registrables** (`register_auth_strategy`, punto D5) none/bearer/oauth; oauth instancia el `OAuthClientProvider` del SDK `mcp` (httpx.Auth con descubrimiento/PKCE/refresh/resource indicator) — NO se reimplementa el flujo, exige `token_storage`. (5) **`McpTool` defaults conservadores para terceros**: `requires_permission=True` siempre, `safe_for_background=read_only` (solo si `readOnlyHint`), **`deferred=True`** (no se anuncia hasta ToolSearch, pero ejecutable desde el pool — M3, concuerda con T7); `McpCall` es el contrato de transporte que el shell NO implementa (lo inyecta el client real); `execute` hace UNA llamada, `isError`→`McpToolError`→`ToolResult.error`, nada propaga. (6) **client** elige transporte por identidad ya validada (stdio/sse/http Streamable), todo en `AsyncExitStack`, connect/aclose en el MISMO contexto async (anyio cancel scopes). (7) **resources**: `tools()` añade `ListMcpResources`/`ReadMcpResource` SOLO si hay resources (read-only, sin permiso, background-safe; acceden a `McpState`, no a loader global). (8) **dos stores inyectables** sobre `StorageProtocol` (T14): `McpConfigStore` (registro `{name:raw}`) y `StorageBackedTokenStorage` (TokenStorage del SDK, tokens+client_info por server para no re-autorizar). (9) **lo que inyecta el integrador** (tabla): servers, storage, config_store, redirect/callback handlers (runtime headless, NO abre navegador), client_factory (stub tests), user_id; el integrador MAPEA su JSON al contrato ([[integrator-maps-to-contract]]). `active_context`/`compact_context` devuelven `[]` (MCP no aporta contexto volátil). TOC del índice ya tenía el wikilink de capability-mcp (#16, sin cambio). Verificación = lectura, sin ejecutar nada.
- 2026-06-18 — T15 — `wiki/capabilities-core.md` escrita y verificada por lectura de los 4 archivos (`capabilities/{contracts,manager,protocol,resolver}.py`) + `capabilities/__init__.py` y el cableado real en `loop/agent_loop.py:71-184` y `factory.py:107-192` (grep de uso por todo el paquete). **Eje rector de la página: el paquete tiene DOS mecanismos de resolución coexistiendo** y el loop los escalona explícitamente. (1) El **vivo** — `CapabilityManager` + `CapabilityProvider` (`contracts.py`): el provider es un Protocol **estructural** `@runtime_checkable` (forma, no herencia), superficie = `name`/`startup`/`shutdown`/`catalog`/`tools`/`active_context`/`compact_context` + el **hook OPCIONAL** `system_prompt_section` (fuera del contrato estructural, invocado tolerante vía `getattr` para no romper terceros; texto estable cache-friendly ≠ contexto volátil de `active_context`). Salidas SIEMPRE tipadas: `CapabilitySummary` (entrada de catálogo) y `CapabilityActivation` (que **NO genera `agent_id` ni copia mensajes** — herencia/fork la decide el runtime). El manager **no conoce ningún provider en concreto**, une en orden de registro y **dedup de tools por nombre first-wins**; `system_prompt_sections` recoge el hook tolerante; **abrir red es del manager (`startup`), no del factory**. (2) `build_tool_pool(native, ctx)` = **punto de convergencia native↔capability** (análogo de `assembleToolPool`): el caller pasa lo nativo ya filtrado por kind, el manager aporta `self.tools(ctx)`, y **la fusión real la hace `ToolPool.assemble()` — único punto** (prefijo built-in, native-gana, deny); dirección de dep capabilities→tools (import perezoso). (3) El **legacy** — `CapabilitiesResolver` + `protocol.py` (`ResolvedCapabilities` opaco solo-schemas, `SkillCatalogProtocol`, `CapabilitySource` con `register_source` como primitiva de extensión): `resolve()` filtra por **KIND no por `is_backgrounded`** (subagente→background, promover no re-filtra), nativas sin timeout, fuentes externas con **timeout parcial 5s + degradación graceful** (lenta→partial, rota→omitida). (4) Hallazgo que cierra: el loop (`agent_loop.py:175-184`) **escalona** — si hay manager/registry usa `build_tool_pool` + schemas DERIVADOS del pool; **`elif` resolver = "Path legacy (solo schemas)"** que SOLO anuncia (el dispatcher ejecuta de `ctx.tool_pool`, que el legacy no llena → no ejecuta por esa vía). El factory cablea AMBOS pero el resolver nunca se consulta si hay manager. TOC del índice ya tenía el wikilink de capabilities-core (#15, sin cambio). Verificación = lectura, sin ejecutar nada.
- 2026-06-18 — T14 — `wiki/storage.md` escrita y verificada por lectura de los 3 archivos de `storage/` (`protocol,filesystem,factory`) + `storage/__init__.py` y `contracts/storage.py`. Eje rector de la página: **no confundir las dos fronteras de nombre parecido** — `StorageProtocol` (este paquete, KV durable por clave string: upload/download/presign/delete/exists/list_prefix) **≠** `StorageContract` (`contracts/storage.py`, resolución de rutas + I/O de tools: real_path/ensure_local/commit/teardown); tabla comparativa explícita que retoma el aviso de T3. Hallazgos que dirigen la página: (1) `StorageProtocol` es **estructural** (`@runtime_checkable`) y su superficie es la de un **object store** (clave→bytes + presign + list_prefix), no POSIX, para que un backend cloud la cumpla sin adaptación; `upload` **devuelve la clave**, no la ruta. (2) `FilesystemStorage(root)` = backend nativo dev/tests con tres invariantes leídas: **anti-traversal** (`_path` resuelve y exige prefijo `root` → `ValueError`), todo I/O en `run_in_executor` (no bloquea el loop), `_write` hace mkdir del padre; **particularidades**: `presign` IGNORA `ttl` y devuelve `file://` URI, `list_prefix` normaliza `\`→`/` y filtra `is_file()` (prefijo inexistente → `[]`, no lanza), y **`copy()` está FUERA del protocolo** (extra `shutil.copy2`, depende del backend concreto, no del puerto). (3) `StorageRegistry` = registry de clases por nombre, `create` da **instancia fresca** (no singleton), backend nativo se **autorregistra** al importar (`register("filesystem", FilesystemStorage)`); patrón D5 (proyecto registra `s3` sin tocar runtime). (4) `StorageKeys` = taxonomía determinista sin dependencia de backend (memo B4): base por agente (`main` en raíz `<uid>/<sid>`, subagentes anidados en `subagents/<agent_id>` — concuerda con identidad de T11), **dos scopes** (config scope-user vs conversación+artefactos scope-user+session) y **dos planos por clave** (conversación = `session.json` transcript + sidecar mutable `session.meta.json` donde vive `is_backgrounded`; artefactos = `work/<filename>` scope compartido). (5) Transcrita la **nota abierta** del código: el mapeo de la cascada de 4 niveles del canónico al modelo cloud está pendiente; `config_key` cubre solo nivel user hoy. TOC del índice ya tenía el wikilink de storage (#14, sin cambio). Verificación = lectura, sin ejecutar nada.
- 2026-06-18 — T13 — `wiki/hooks.md` escrita y verificada por lectura de los 3 archivos de `hooks/` + sus call sites (`factory.py:82,196` y `execution/local/runtime.py`) y un `grep` de cableado sobre todo el paquete. Eje conceptual de la página: **hooks = control (deciden), EventBus = observación (solo emite)** — un handler recibe payload y devuelve `HookDecision` que puede block/stop/modified_input/additional_context. Hallazgos que dirigen la página: (1) **taxonomía completa pero cableado mínimo** — los 11 `HookEvent` (PascalCase canónico, tomados de `coreSchemas.ts:355`) están definidos, pero el `grep` confirma que el runtime **solo dispara `SUBAGENT_STOP`**; los otros 10 (PreToolUse/PostToolUse/UserPromptSubmit/Session*/Stop*/PreCompact/Notification) existen como contrato + punto de extensión D5 y nadie los invoca. (2) `HookRunner.run` siempre devuelve un `HookDecision` (nunca None): aísla cada handler en try/except (uno roto no rompe el ciclo, igual que el EventBus), **corta al primer block/stop** devolviéndolo tal cual, y si nadie corta agrega → `modified_input` se queda con el ÚLTIMO, `additional_context` se CONCATENA con `\n`. (3) `register_sink` sin eventos = suscrito a TODOS (`events or tuple(HookEvent)`). (4) Inyección: `hook_runner` lo provee el consumidor (`RuntimeConfig.hook_runner: Any=None` → `LocalAgentRuntime._hook_runner`); si None, todo disparo es no-op. (5) El único disparo, `_fire_stop` (runtime.py:263-270), se llama en las 3 salidas de `_run_loop` (killed/failed/completed) y **NO usa** el `HookDecision` devuelto (es notificación, no control); subrayado que es mecanismo DISTINTO del observer `SubagentStarted/Stopped` (T11) y del canal de notificación background (T10). TOC del índice ya tenía el wikilink de hooks (#13, sin cambio). Verificación = lectura, sin ejecutar nada.
- 2026-06-18 — T12 — `wiki/modos-senales.md` escrita y verificada por lectura de los 4 archivos (`modes/{protocols,manager}.py`, `signals/{protocols,bus}.py`) + contraste de cableado por `grep` sobre todo el paquete y `tests/test_modes_background.py`. Hallazgo que dirige la página: **ninguna de las dos primitivas está cableada todavía** — ni `ModeManager` ni `SignalBus` se instancian/referencian fuera de sus paquetes y tests; el runtime gobierna abort por la `asyncio.Event` `ctx.stop` (fork, T11/T10) y el background por el flag `is_backgrounded` del `TaskRegistry` (T11), no por estas piezas. Se documentan como **contrato + punto de extensión D5**, marcando qué hacen y qué NO gobiernan aún. Decisiones subrayadas: (1) `AgentMode` (FOREGROUND/BACKGROUND/FORK) con dos guardas en `set_mode`: modo actual `FORK` → `ValueError` (inmutable), y los listeners disparan SOLO en transición real (`current is not None`) — el alta inicial NO dispara (asimetría a recordar); `on_complete` = True sii BACKGROUND (intención que coincide con el canal de notificación de T10, que sin embargo NO consulta este manager). (2) Aviso anti-confusión: `RuntimeFactory._modes` (`factory.py:100`, T2) es un **registro de clases de runtime por string `execution_mode`**, homónimo SIN relación con `AgentMode`/`ModeManager`. (3) `SignalBus` = árbol parent→children (misma topología del fork, replicada como árbol de señales): `send(cascade=True)` propaga a todos los descendientes (DFS `_children`); `RESUME` es especial — `_apply` lo traduce a `signal=None` (anula PAUSE/ABORT, no es estado persistente); el ejecutor hace **polling** vía `SignalHandle.check()`, el bus no empuja; `unregister` de un padre NO toca a los hijos. (4) Extension primitives D5: `ModeManager.on_transition` (callback por cambio de modo) y `SignalBus.register_handler` (adjunta `SignalHandler` por push vía `object.__setattr__` sobre el `_Node`, lanza `KeyError` si no registrado) — pero el bus ALMACENA los handlers y NO los invoca en `send` hoy (gancho a activar por el integrador). TOC del índice ya tenía el wikilink de modos-senales (#12, sin cambio). Verificación = lectura, sin ejecutar nada.
- 2026-06-18 — T11 — `wiki/sesiones-tareas.md` escrita y verificada por lectura de los 7 archivos (`execution/session/{protocol,session}.py`, `execution/tasks/{registry,status}.py`, `execution/observer/{events,observer}.py`, `execution/fork/__init__.py`, `execution/context/agent_context.py`). La página se organiza sobre la firma de diseño común — **protocolo de frontera + default nativo + primitiva `set_*`/`get_*`** — y subraya las asimetrías que importan: (1) `RuntimeSessionProtocol` = 6 atributos planos sin métodos (mínimo que el motor exige), satisfecho por `Session` (Pydantic) vía **propiedades planas** que proyectan `usage`/`metadata`; `Session` **no** pinta (`display_messages` es del consumidor por EventBus, G2/D4) ni persiste (eso es del runtime vía StorageProtocol). (2) `BackgroundTaskRef` (vista del padre) ≠ `TaskRecord` (expediente en el registry) — dos representaciones de la misma tarea en planos distintos. (3) `is_backgrounded` es eje **mutable relativo al observador** que **NO afecta al toolset** (eso es por `kind`, B3) — "background" = visibilidad, no capacidad. (4) Asimetría clave a recordar: `get_registry()` **lanza `RuntimeError`** si nadie inyectó (registry obligatorio) mientras `get_observer()` devuelve `NoopObserver` default (observar es opcional); `arm_watchdog` del registry en-proceso es no-op deliberado (el watchdog real lo arma `LocalAgentRuntime`, T10). (5) Observer (`SubagentStarted/Stopped`, frozen) es mecanismo **separado del EventBus** (T6): ciclo de vida de subagentes visto desde el padre, no eventos del turno. (6) Fork: `ForkPolicy.inherit_messages` default **False** (hijo sin historial), `ForkSnapshot` frozen con `messages` como tuple; `RuntimeContextForker.fork` genera **agent_id nuevo siempre** pero **hereda session_id** (sesión compartida, identidad distinta), y `propagate_abort` comparte la MISMA `asyncio.Event` del padre → cascada de abort. (7) `agent_id`/`AgentContext` resuelven identidad de subagentes concurrentes vía `ContextVar` (copiado al crear cada `asyncio.Task` → aislamiento), mismo mecanismo que `ExecutionContext` (T9) enfocado a identidad/`user_id`. TOC del índice ya tenía el wikilink de sesiones-tareas (#11, sin cambio). Verificación = lectura, sin ejecutar nada.
- 2026-06-18 — T10 — `wiki/ejecucion.md` escrita y verificada por lectura de los 4 archivos (`execution/runner.py` + `execution/local/{runtime,notification,summarizer}.py`). Decisiones que dirigen la página: (1) `LocalAgentRuntime` es la **implementación local del contrato `AgentRuntime`** que compone primitivas inyectadas (cualquiera puede ser `None` → runtime degradado) y **no conoce consumidor** (identidad = `owner_id` opaco, padre = `ForkSnapshot`); `startup/shutdown` delegan en el `capability_manager` y son **lo que abre red** (el factory no). (2) `dispatch` **no bloquea** — lanza `_run_loop` en `ensure_future`, arma watchdog (`task.timeout_seconds or default 300s`) y devuelve `task_id`; `stream` es azúcar sobre `dispatch(on_event=...)` y **no pierde eventos** porque el centinela se encola en el `add_done_callback` que dispara tras el `emit` awaiteado dentro de `run()`. (3) `_build_child` reparte identidad en dos ramas — subagente forka del snapshot (depth+1, hereda session_id) vs. principal con ids nuevos donde se **siembran `initial_allowed_tools`** (sin esto `write_file`/memoria quedan fuera del pool del agente autónomo; los subagentes los heredan por snapshot). (4) `_make_bus` registra **primero** al propio runtime (push_event al TaskRegistry, output **truncado a 2000**) y luego el `on_event` del integrador vía `subscribe_all` (concuerda con T6). (5) `runner.py` = **frontera mínima `set_runner/get_runner`** para que `AgentTool` lance subagentes sin importar el runtime (rompe el ciclo de deps); `run(..., background)` foreground bloquea/background lanza task. (6) Canal de notificación = `defaultdict` en memoria por `parent_session_id`: el hijo `put_notification` al terminar (sin ref viva a la `Session` del padre), el padre `drain_notifications` en turn-start (DrainableLoop, T4) y `process_background_notification` inyecta `<task-notification>` + actualiza el ref — **display/persistencia NO son su responsabilidad** (G2/D4). (7) Hallazgo subrayado: el **summarizer NO es la compactación de conversación** (eso es `CompactionProvider`, T3) — solo condensa el *resultado de un subagente background* (`background_result_max_chars=2000`, `small_llm`) con **degradación graceful** (sin/llm roto → texto intacto, el resultado nunca se pierde). TOC del índice ya tenía el wikilink de ejecucion (#10, sin cambio). Verificación = lectura, sin ejecutar nada.
- 2026-06-18 — T9 — `wiki/contexto.md` escrita y verificada por lectura de los 4 archivos de `context/`. La página gira sobre la distinción dura **dos objetos de contexto**: (1) `ToolUseContext` (Pydantic, mutable, **por turno**, viaja explícito al loop/dispatch/tools) vs. `ExecutionContext` (dataclass **inmutable**, identidad por-ejecución, viaja **ambiental** vía `ContextVar` privado `_ctx`, no por argumento). Hallazgos que dirigen la página: (a) `ToolUseContext` transporta la frontera inyectable (`storage`/`presentation`/`exec_env`) como `Any` a propósito (Protocols sin acoplar clase), y separa `messages` (mutado in-place por el loop) de los canales async `stop`/`event_queue`; (b) `AppState` tiene **tres planos** — `permissions`, `capabilities` (buzón por-capability), `native` (buzón nativas) — ambos buzones fuera del historial y scopeados al turno; `permission_context` es property→`app_state.permissions` y `with_permissions` devuelve **copia** (`model_copy`, inmutable); (c) `run_with_context` crea un **nuevo asyncio.Task** que copia el ContextVar y sobreescribe `_ctx` → ejecución hija **aislada del padre** (mecanismo de identidad de subagentes/forks; `parent_id` enlaza); `RuntimeState` es lo opuesto: estado global mutable compartido (`foreground_task_id`); (d) `IdentityPresentation` = default no-op del `PathPresentation` (contrato en `contracts/storage.py`), el **choke point** por donde un integrador de infra mapea rutas host→tokens falsos (`sanitize_output` aplicado en el dispatcher antes de messages Y bus — concuerda con T7); (e) `adapters.py` es **shim de transición** Session↔ToolUseContext (todo por `getattr` defensivo; `apply_context_modifier_compat` tolera modifiers nuevos sobre-contexto y viejos sobre-sesión con fallback `AttributeError`) — el integrador nuevo no lo necesita. TOC del índice ya tenía el wikilink de contexto (#9, sin cambio). Verificación = lectura, sin ejecutar nada.
- 2026-06-18 — T8 — `wiki/tools-nativas.md` escrita y verificada por lectura de los 17 módulos de `tools/native/` (+ `__init__.py`). Decisiones que dirigen la página: (1) **17 módulos → 22 clases** (plan_mode×2, worktree×2, task_tools×6) — la página cataloga por categoría, no por archivo. (2) Eje transversal **permiso**: solo 7 piden `requires_permission` (`bash`, `write_file`, `Edit`, Enter/ExitWorktree, WebFetch, WebSearch); **las de lectura `read_file`/`glob`/`grep` NO** (concuerda con el "modelo autónomo" del loop, que solo anuncia lo permitido). (3) **`safe_for_background=False`** exactamente en las interactivas/sesión-principal: plan_mode×2, worktree×2, AskUserQuestion. (4) Patrón **`context_modifier`** (no mutación directa de ctx) en TodoWrite/Config/plan_mode/worktree → estado en `app_state.native[<clave>]`, scope sesión, fuera del historial. (5) Hallazgo subrayado: las 5 tools de archivo tocan FS **directo vía `Path`**, sin `StorageContract` ni presentación de rutas (el saneo está aguas abajo en `dispatch`). (6) Particularidades documentadas: `bash` usa `ctx.exec_env` inyectable; `Edit` exige path absoluto y unicidad de `old_string`; `Agent` siempre hereda permisos+pool y tope depth=5; `EnterPlanMode` se rechaza en subagente (`agent_id`); worktree opera git real por subprocess con slug regex; `WebFetch` solo http/https (CWE-22); `WebSearch` requiere `SERPER_API_KEY`; `Sleep` capa a 60 s; `ToolSearch` solo diferidas. (7) Nomenclatura mixta minúscula/CamelCase documentada tal cual (es la clave de resolución). TOC del índice ya tenía el wikilink de tools-nativas (#8, sin cambio). Verificación = lectura, sin ejecutar nada.
- 2026-06-18 — T7 — `wiki/tools-core.md` escrita y verificada por lectura de los 8 archivos de `tools/` + `tools/native/tool_search.py` (cierra el flujo deferred). Ejes verificados: (1) **`ToolProtocol` es estructural** (`@runtime_checkable`, atributos planos + `execute`) → nativas y adaptadores MCP/Skills son uniformes en el pool sin clase base; `ToolResult` lleva banderas error/timeout/aborted con constructores que usa el dispatcher. (2) **Pool particionado** (`native_tools`/`capability_tools`) que se fusiona en `assemble_tool_pool` con tres invariantes leídas en el código: ordenado por nombre (caché de prompt), **deny aplicado en el ensamblado** (lo denegado no entra), **native gana en colisión** (van primero + `seen`); `find` resuelve del pool ENSAMBLADO = `findToolByName`. (3) **Dispatcher = punto único**: secuencia abort→find→permiso→timeout(override>dispatcher>tool)→`validate_tool_arguments`→`wait_for(execute)`, y **nada propaga** (toda falla → `ToolResult`); el **choke point de presentación** está en `dispatch` (no `_run`): `sanitize_output` antes de messages Y bus. (4) **Deferred = visibilidad, no disponibilidad** — `is_deferred_tool` (ToolSearch nunca, MCP siempre, nativas solo si `deferred=True`), estado descubierto scopeado por agente en `ctx.app_state.capabilities["discovered_tools"]` (no en historial, a diferencia del canónico), y `ToolSearchTool` filtra SOLO diferidas, marca-descubre y devuelve schema completo; una diferida no descubierta sigue ejecutable porque `find` mira el pool. (5) **Dos registries ≠ pool**: `ToolRegistry` (extensión del integrador, `list_available` filtra background) y `NativeToolRegistry` (agnóstico, `unregister_by_prefix`); `create_tools(extras)` arma el registry nativo. (6) **`exec_env` inyectable**: `ToolExecEnvironment` de un método; Local default, Bwrap monta `/workspace` real (sin reescritura), remoto vía D5c. TOC del índice ya tenía el wikilink de tools-core (#7, sin cambio). Verificación = lectura, sin ejecutar nada.
- 2026-06-18 — T6 — `wiki/eventos.md` escrita y verificada por lectura de los 3 archivos de `events/` + sus call sites en `loop/agent_loop.py` y `execution/local/runtime.py`. La página gira sobre tres ejes verificados: (1) **frontera mínima** — `events/protocol.py` solo define `Event` (dataclass frozen vacía, base de todo), `EventHandler` (corrutina) y `EventBusProtocol` (subscribe/subscribe_all/emit); el `__all__` de `__init__.py` cierra el catálogo en 5 eventos (Token/ToolCall/ToolResult/Done/Error), con `Usage` viajando *dentro* de `DoneEvent`, no como Event. (2) **`subscribe` resuelve por tipo EXACTO** (`emit` usa `type(event)`, no `isinstance`) → un handler de `Event` base no recibe subtipos; `subscribe_all` es la vía para el stream completo (SSE). Cada handler se aísla en try/except: uno roto no tumba el stream. (3) **origen de los eventos**: los 4 primeros nacen en el caller del modelo (traduce su SDK), el loop los reemite tal cual en `_emit` (turno 220, antes de procesarlos); **`ToolResultEvent` es la excepción — lo fabrica el loop** tras `dispatch` (270). Hallazgo que dirige el flujo del integrador: este NO toca el bus — usa `dispatch(on_event=...)`/`stream(task)` del contrato; internamente `_make_bus` registra PRIMERO los suscriptores del propio runtime (push_event al task_registry, output truncado a 2000) y luego añade el `on_event` del integrador vía subscribe_all → el runtime es su propio primer consumidor. Subrayado el no-confundir con el observer de subagentes (mecanismo aparte). TOC del índice ya tenía el wikilink de eventos (#6, sin cambio). Verificación = lectura, sin ejecutar nada.
- 2026-06-18 — T5 — `wiki/modelos-caller.md` escrita y verificada por lectura de `models/{protocol,caller}.py` + el call site en `loop/agent_loop.py:201-211`. La frontera es de **un solo método**: `ModelCallerProtocol.complete` — corrutina que *devuelve* un async generator (el loop hace `stream = await complete(...)` y luego itera), con mensajes/tools como `dict` planos y emitiendo `Event` del runtime (cero tipos de SDK cruzan la frontera). Hallazgos que dirigen la página: (1) **`ModelRequest` está definido y exportado pero NO cableado** — el loop llama `complete(ctx.messages, tool_schemas, **kwargs)` con args sueltos, no construye un `ModelRequest`; es tipo de conveniencia/futuro (su `thinking_budget` no lo usa el loop hoy); (2) reparto estricto en `system_sections`: el **runtime ensambla** (`capability_manager.system_prompt_sections` por turno, kwarg solo si no vacío por robustez ante callers viejos) y el **caller transporta** (`_compose_system_prompt` concatena `[base, *sections]` con `\n\n`, secciones al final para prefijo de caché estable); (3) `model_id` por request **manda** sobre el `model` del constructor, y un id desconocido es **error explícito** (`get_model` lanza `ModelNotFoundError`, no cae al default); (4) `AgenticModelsCaller` es el puente a `agentic_models` con imports **perezosos** (no acopla en import-time), traduce dict↔`Context` en ambos sentidos y mapea eventos (`text_delta`→Token, `toolcall_end`→ToolCall, `done`→Done con `toolUse`→`tool_calls`, `error`→Error; resto ignorado). TOC del índice ya tenía el wikilink de modelos-caller (#5, sin cambio). Verificación = lectura, sin ejecutar nada.
- 2026-06-18 — T4 — `wiki/loop.md` escrita y verificada por lectura de los 4 archivos de `loop/`. El núcleo del runtime es delgado: `LoopProtocol.run` no retorna — muta `ctx.messages` (estado) y emite al bus (stream), dos canales deliberadamente separados (adaptación del canónico que combina ambos en un yield). Hallazgos que dirigen la página: (1) el pool se **reensambla en cada turno** (`_build_tool_pool`) y es la fuente única — schemas anunciados y dispatcher resuelven del MISMO `ctx.tool_pool`; (2) `_schemas_for_turn` aplica dos filtros no obvios: permiso (la tool con `requires_permission` solo se anuncia si está permitida → modelo autónomo) y diferidas M3 (MCP oculto hasta que ToolSearch lo descubre, aunque vive en el pool); (3) el `context_modifier` que devuelve una tool muta `ctx` in-place (no forka) y, como el pool se reensambla al turno siguiente, sus cambios (permisos/modo/skill activa) se reflejan solos; (4) cierre por 4 vías (natural `stop_reason != "tool_calls"` / error / abort `ctx.stop` / techo `_MAX_TURNS=50`). `DrainableLoopProtocol` = los turn-start hooks (drenado de notificaciones background). `create_loop`/`basic.py` son para el consumidor (D5c); el runtime construye su loop directo. TOC del índice ya tenía el wikilink de loop (#4, sin cambio). Verificación = lectura, sin ejecutar nada.
- 2026-06-18 — T3 — `wiki/contratos.md` escrita y verificada por lectura de los 5 archivos de `contracts/` + sus defaults. El catálogo es **cerrado**: `contracts/__init__.py` reexporta exactamente 8 nombres = la superficie pública de acoplamiento. Hallazgo que dirige la página: "quién implementa" no es binario runtime/integrador — varios contratos los implementan las **capabilities** (skills implementa `UserInputProcessor`; skills/mcp/memoria implementan `CompactionProvider`). Distinciones verificadas y subrayadas: (1) `StorageContract` (path+IO de tools, `contracts/storage.py`) **≠** `StorageProtocol` (KV durable, `storage/protocol.py`, T14) — nombres parecidos, fronteras distintas; (2) `StorageContract` **no tiene default** en el runtime (frontera pura, viaja por `ToolUseContext`), mientras `PathPresentation` sí trae `IdentityPresentation` y `UserInputProcessor` trae `NoopUserInputProcessor`; (3) `PermissionContext` y `RuntimeTask` no son Protocols (modelo Pydantic / dataclass) — el runtime posee el primero, el integrador construye el segundo. Confirmado por grep que `StorageContract` no se referencia fuera de `contracts/`+tests (cero default interno). TOC del índice ya tenía el wikilink de contratos (sin cambio). Verificación = lectura, sin ejecutar nada.
- 2026-06-18 — T2 — `wiki/factory.md` escrita y verificada por lectura de `factory.py` + `contracts/runtime.py`. El gate de cada capability/voz vive en el factory, no en el runtime: providers MCP/Skills/Memoria se registran *solo si* su config (servers/dir/store/root) está presente, y un canal de voz queda activo *sii* primitiva inyectada **y** flag `*_enabled`. Clave verificada: el factory **no** abre red (MCP conecta en `manager.startup()`, lo invoca el integrador); ningún campo es obligatorio para construir, pero `model_caller` lo es en la práctica e `initial_allowed_tools` es la concesión espejo del canónico (la memoria no se auto-concede `write_file`). `RuntimeFactory._modes` es la extension primitive (vía D5-b). TOC del índice ya tenía el wikilink de factory (sin cambio). Verificación = lectura, sin ejecutar nada.
- 2026-06-18 — T1 — `wiki/arquitectura.md` escrita y verificada por lectura. "Qué es/no es" sale de `__init__.py` (reexporta solo el contrato público, no internos de capabilities) y `contracts/runtime.py` (sin entry point/HTTP; identidad = `owner_id` opaco). Los "principios D1–D5" del criterio resultaron ser las **decisiones** de `PLAN_COMPLEMENTARIO_RUNTIME.md` §2 (no había una lista "D1–D5" en el plan principal, que solo trae "Principio Rector"); se documentan como tales, con D5 = tres vías de extensión. Capas mapeadas 1:1 con la estructura real del paquete + el flujo de turno extraído del ciclo real de `AgentLoop` (Fase 20). TOC ya tenía el wikilink (no requirió cambio). Verificación = lectura, sin ejecutar nada.
- 2026-06-18 — E0 — Índice del vault corregido: repo path real (`agentic_runtime`, antes `agent-runtime`), versión real `0.1.0` (antes 1.0.0, inventada), estado de tests/lint tomado del estado conocido del Plan 2 (354 passed; ruff+mypy+bandit), y TOC con las 19 técnicas + 7 de uso como wikilinks por crear. Decisión del usuario: la documentación **no corre tests ni lint nunca** → protocolo actualizado aquí y en la memoria [[docs-obsidian-plan]] (verificar = leer, no ejecutar).
